{
  "name": "ckm-catering",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "check": "node devops/run_content_check.mjs",
    "build": "npm run check && astro check && astro build",
    "//build:deploy": "For build environments without Python 3 (e.g. a Cloudflare Pages image that lacks it). Content is already gated by the content-gate CI workflow before anything reaches main, so this skips only a check that has already run — it is not a way to bypass the gate locally.",
    "build:deploy": "astro check && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "@playwright/test": "^1.61.1",
    "astro": "^6.1.8",
    "rss-parser": "^3.13.0",
    "tailwindcss": "^3.4.0"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.8",
    "@astrojs/sitemap": "^3.7.2",
    "@lucide/astro": "^1.23.0",
    "@tailwindcss/typography": "^0.5.20",
    "typescript": "^6.0.3"
  }
}
